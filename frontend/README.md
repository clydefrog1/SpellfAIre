# Spellfaire

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.1.4.

## Development server

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Code scaffolding

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
```

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

```bash
ng generate --help
```

## Building

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.

## Running unit tests (Karma)

To execute unit tests with Karma + Jasmine, use:

```bash
npm run test -- --watch=false --browsers=ChromeHeadless
```

## Running end-to-end tests (Playwright)

Install browser binaries (once per machine):

```bash
npm run e2e:install
```

Run mocked API e2e suite:

```bash
npm run e2e:mock
```

Run real-backend e2e suite (requires backend on `http://localhost:8080`):

```bash
npm run e2e:real
```

## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.

## Assets & Licenses

This project includes original SVG artwork and textures in `public/assets/fantasy/` created for the SpellfAIre fantasy theme.

### Fantasy Assets

- **parchment-texture.svg** — Original SVG pattern, CC0 (public domain)
- **rune-ornament.svg** — Original SVG pattern, CC0 (public domain)
- **rune-ornament-forest.svg** — Original SVG pattern, CC0 (public domain)
- **vignette-overlay.svg** — Original SVG pattern, CC0 (public domain)

All fantasy assets are original creations and are provided under the CC0 license, permitting unrestricted use, modification, and distribution without attribution.
